---

- hosts: localhost
  remote_user: root
  vars:
    jabberd2_router_user_secret: foobar
    jabberd2_component_secret: foobar2
    jabberd2_domain: foobar.localdomain
    jabberd2_mysql_user: root
    jabberd2_mysql_password: password
  roles:
    - ansible-role-jabberd2
